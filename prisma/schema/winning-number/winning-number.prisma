/**
 * 당첨번호
 */
model WinningNumber {
  episode Int     @id // 로또 회차
  numbers String // 당첨번호 SET
  isDrawn Boolean @default(true)

  algorithmResult AlgorithmResult[] // 회차 별 각 알고리즘에 대한 결과

  @@map("winning_number")
}
